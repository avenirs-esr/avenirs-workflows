name: Deploy on remote server
on:
  workflow_call:
    inputs:
      target_env:
        required: true
        description: "Target environment for deployment (dev, qualif, recette)"
        type: string
        default: dev
      tag_name:
        required: false
        description: "Tag name for the deployment (if needed)"
        type: string
        default: ""


jobs:
  deploy:
    runs-on:
      group: Default
      labels:
        - self-hosted
        - ${{ (inputs.target_env == 'recette' && 'srv-recette') || (inputs.target_env == 'qualif' && 'srv-qualif') || 'srv-dev' }}
    permissions:
      contents: read

    steps:
      - name: ✅ Checkout
        uses: actions/checkout@v4

      - name: 🚀 Deploy application on remote server
        uses: avenirs-esr/avenirs-workflows/.github/actions/deploy-on-server@main
        with:
          srv_base_path: ${{ secrets.SRV_BASE_PATH }}
          srv_deploy_cmd: ${{ secrets.SRV_DEPLOY_CMD }}
          jasypt_encryptor_password: ${{ secrets.JASYPT_ENCRYPTOR_PASSWORD }}
          java_version: ${{ vars.JAVA_VERSION }}
          maven_version: ${{ vars.MAVEN_VERSION }}
          conf_path: ${{ vars.CONF_PATH }}
          conf_file: ${{ vars.CONF_FILE }}
          tag_name: ${{ inputs.tag_name }}
